# 秋野案例详情覆盖规则

- 延续首页的白灰底、黑色正文、蓝色操作色与案例主题色，不使用自动推荐的紫粉模板配色。
- 详情页顺序固定为封面、项目说明、能力标签、思路、图库、下一个案例；真实边界文案必须保留。
- 图库缩略图保留固定宽高，点击后用原比例大图；对话框必须支持关闭按钮、Escape、左右方向键并把焦点还给触发按钮。
- 详情页路由使用 `#/work/<slug>`，可直接访问、浏览器返回并恢复首页滚动位置。
